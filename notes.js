//To preserve text area paragraphs, 
//we must set the model of the data type of the ORM to be text area
//but of course, to properly interpret text area fields, we need a text area input type
//this is implemented by the <textarea> html tags which is then sent with the req.body when a POST is made. np